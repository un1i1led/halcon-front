export interface Customer {
  id: number;
  name: string;
  fiscalData: string;
  address: string;
  phone: string;
  customerNumber: string;
  deleted: boolean;
  createdAt: string;
  updatedAt: string;
}